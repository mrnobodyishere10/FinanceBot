```
# .env (Preserved)
# Configuration variables for FinanceBot
# Example:
# API_KEY=your_api_key
# DATABASE_URL=your_database_url

# .env.example (New)
API_KEY=your_api_key
DATABASE_URL=your_database_url

# README.md (Consolidated)
# FinanceBot Documentation
## Overview
FinanceBot is a bot designed to assist with financial tasks.

## Setup
1. Clone the repository.
2. Run `npm install` to install dependencies.
3. Create a `.env` file using `.env.example` as a template.

# build.sh (Preserved)
#!/bin/bash
# Build script for FinanceBot
npm install
npm run build

# sample.js (Preserved)
console.log("Sample script for FinanceBot");
```
Dengan perubahan ini, repositori akan lebih terorganisir dan mudah dipahami, serta mengurangi risiko kesalahan konfigurasi.